// Import / Export Statements - pt2
// 'add' is exported by default and can be
// directly named differently at the destination

// there cannot be more than 1 export default
//export default function add(a, b) {
//  return a + b;
//}
